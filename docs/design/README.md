# Design records

ADR-style decision records, flat and dated: `yyyy-mm-dd-<slug>.md`. Append-mostly
— after acceptance only `status`/`superseded-by` change. Schema:
aivolution-meta `docs/schemas/design-record.md`.
