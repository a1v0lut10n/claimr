# Architecture aspects

Cross-component invariants, one per aspect, filename = canonical aspect name (see
[`../README.md`](../README.md) for the vocabulary). Mutate-in-place. Schema:
aivolution-meta `docs/schemas/architecture-aspect.md`.
