# Architecture components

Current-state docs, one per component, filename = canonical component name (see
[`../README.md`](../README.md) for the vocabulary). Mutate-in-place. Schema:
aivolution-meta `docs/schemas/architecture-component.md`.
